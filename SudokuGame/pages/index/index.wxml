<view class='container'>
  <view class='rows {{rowGroupClasses[index%3]}}' wx:key="rows{{index}}" wx:for="{{dataSource}}" wx:for-item="row">
    <view class='cols {{colGroupClasses[index%3]}}' wx:key="cols{{index}}" wx:for="{{row}}" wx:for-item="col">
      {{col}}
    </view>

  </view>
</view>